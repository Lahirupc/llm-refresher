[project]
name = "fastapi-expose-llm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Lahirupc", email = "lahirupc@yahoo.com" }
]
requires-python = ">=3.14"
dependencies = [
    "dotenv>=0.9.9",
    "fastapi[standard]>=0.141.1",
    "langchain>=1.3.16",
    "langchain-google-genai>=4.3.5",
]

[project.scripts]
fastapi-expose-llm = "fastapi_expose_llm:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
